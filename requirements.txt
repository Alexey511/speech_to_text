# Core ML/DL framework
torch==2.5.1
torchaudio==2.5.1
torchvision==0.20.1

# Configuration management
omegaconf==2.3.0
hydra-core==1.3.2

# Transformers and pre-trained models
transformers==4.57.0
sentencepiece==0.2.1  # Required for Speech2Text tokenizer
datasets==4.2.0
accelerate>=0.26.0  # Required for Trainer with PyTorch (install via conda: conda install -n basenn -c conda-forge accelerate -y)
huggingface_hub==0.35.3

# Data handling
pandas==2.3.3
numpy==2.0.1
scipy==1.15.2

# Visualization and monitoring
matplotlib==3.9.1
seaborn==0.13.2
tensorboard==2.20.0
wandb>=0.15.0  # Install via pip: conda run -n basenn pip install wandb
plotly==6.3.1

# Metrics and evaluation
jiwer>=3.0.0  # Install via conda: conda install -n basenn -c conda-forge jiwer -y
evaluate>=0.4.0  # Install via pip: conda run -n basenn pip install evaluate

# Development tools
jupyter==1.1.1
ipykernel==6.30.1
ipywidgets==8.1.7
pytest==8.4.2
mypy>=1.0.0  # Install via conda: conda install -n basenn -c conda-forge mypy -y
ruff>=0.1.0  # Install via conda: conda install -n basenn -c conda-forge ruff -y

# Utilities
tqdm==4.67.1
pyyaml>=6.0
python-dotenv>=1.0.0  # Install via pip: conda run -n basenn pip install python-dotenv
rich==14.2.0

# Audio backend (required for MP3 support)
# ffmpeg  # Install via conda: conda install -n basenn -c conda-forge ffmpeg -y

# Removed (not used in project):
# soundfile - replaced by torchaudio with ffmpeg backend
# openai-whisper - using transformers Whisper implementation instead
# speechbrain - not used
# black - replaced by ruff
# flake8 - replaced by ruff
# pywin32 - optional Windows-specific
